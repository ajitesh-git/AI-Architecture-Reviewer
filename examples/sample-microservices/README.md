# Sample Microservices Solution

Use this directory to test the web analyzer.

Upload the folder contents or create a zip:

```bash
zip -r sample-microservices.zip sample-microservices
```

Expected findings include missing timeouts, synchronous service calls, and hardcoded secrets.
